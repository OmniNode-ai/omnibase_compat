# SPDX-FileCopyrightText: 2026 OmniNode.ai Inc.
# SPDX-License-Identifier: MIT

# required-checks.yaml v3 (OMN-14858 — fan-out of OMN-14854 omniclaude canary)
# Location: .github/required-checks.yaml
#
# This manifest is the OPERATIONAL input to the Required-Check Skip-Vector
# Guard (validate_no_required_check_skip_vectors.py, canonical source hosted
# in omniclaude, invoked here via required-check-skip-guard-caller.yml).
#
# Exhaustive: every context in required_status_checks.contexts for
# omnibase_compat's `dev` branch has a `mode: REQUIRED` row below (fetched via
# `gh api repos/OmniNode-ai/omnibase_compat/branches/dev/protection/required_status_checks
# --jq '.contexts'`, 21 contexts, verified 2026-07-20). The privileged reconcile
# job (not yet wired here — see follow-up note at end of file) would fail
# closed if this file and live branch protection diverge in either direction.
#
# SCOPE NOTE (mirrors OMN-14854's own scope note): `main`'s required_status_
# checks set is NOT identical to `dev`'s (dev has 21 contexts; main has 17 —
# main additionally omits "Enforce validator-requirements.yaml (OMN-13291)"
# and others per the 2026-07-20 branch-protection union PATCH ledger entry).
# This manifest is single-branch (dev), matching the OMN-14854 precedent. A
# `main`-scoped manifest section is an explicit open follow-up, not resolved
# here.
#
# Do NOT rename/remove a REQUIRED row without following the Branch Protection
# Migration Safety procedure — flip branch protection first (or same PR), then
# update this file, so a future reconcile job never observes a divergence
# window.
#
# `job_path` len 1 = Shape A (direct job); len 2 = Shape B/C
# ([caller_job_id, reusable_job_id]) per the design spec's context->job
# resolution algorithm (OMN-14854 ticket, spec section 1).

schema_version: 3
classification: toolchain
repo: omnibase_compat

gates:
  - name: "CI Summary"
    mode: REQUIRED
    producer_kind: local
    workflow: ci.yml
    job_path: ["ci-summary"]
    skip_semantics: never
    rationale: "Fail-closed no-needs poller aggregating all ci.yml job conclusions. Live required status check on omnibase_compat dev branch protection."

  - name: "call-reject-skip-token / scan / reject-skip-gate-token"
    mode: REQUIRED
    producer_kind: cross_repo
    caller_workflow: call-reject-skip.yml
    caller_job: call-reject-skip-token
    cross_repo_ref: "OmniNode-ai/omniclaude/.github/workflows/reject-deploy-gate-skip.yml@main"
    skip_semantics: never
    rationale: "Skip-token bypass scanner (OMN-10422). Live required status check on omnibase_compat dev branch protection."

  - name: "contract-validation"
    mode: REQUIRED
    producer_kind: local
    workflow: contract-validation.yml
    job_path: ["contract-validation"]
    skip_semantics: never
    rationale: "Live required status check on omnibase_compat dev branch protection."

  - name: "Dep Provenance Gate"
    mode: REQUIRED
    producer_kind: local
    workflow: dep-provenance-gate.yml
    job_path: ["dep-provenance-gate"]
    skip_semantics: never
    rationale: "Live required status check on omnibase_compat dev branch protection."

  - name: "PEP 604 Type Union Check (UP007)"
    mode: REQUIRED
    producer_kind: local
    workflow: omni-standards-compliance.yml
    job_path: ["type-union-check"]
    skip_semantics: never
    rationale: "Live required status check on omnibase_compat dev branch protection."

  - name: "CI Naming Convention"
    mode: REQUIRED
    producer_kind: local
    workflow: omni-standards-compliance.yml
    job_path: ["ci-naming-convention"]
    skip_semantics: never
    rationale: "Live required status check on omnibase_compat dev branch protection."

  - name: "Contract Compliance Check"
    mode: REQUIRED
    producer_kind: local
    workflow: ci.yml
    job_path: ["contract-compliance"]
    skip_semantics: never
    rationale: "Live required status check on omnibase_compat dev branch protection. Job-level `if:` references only github.event_name (pull_request/merge_group/push), provably true for both PR-reachable events declared on ci.yml — resolves ALWAYS_TRUE_FOR_PR, no finding."

  - name: "Tests Gate"
    mode: REQUIRED
    producer_kind: local
    workflow: ci.yml
    job_path: ["tests-gate"]
    skip_semantics: never
    rationale: "Job-level `if: always()` — provably true regardless of upstream job outcome. Live required status check on omnibase_compat dev branch protection."

  - name: "validate"
    mode: REQUIRED
    producer_kind: local
    workflow: ci.yml
    job_path: ["validate"]
    skip_semantics: never
    rationale: "Live required status check on omnibase_compat dev branch protection."

  - name: "ONEX Change Control Schema Compatibility"
    mode: REQUIRED
    producer_kind: local
    workflow: onex-schema-compat.yml
    job_path: ["schema-compat"]
    skip_semantics: neutral_ok
    rationale: "KNOWN FINDING (OMN-14858 guard fan-out build): job 'schema-compat' has a job-level `if: github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository` — an ordinary fork-vs-same-repo guard, but it references github.event.pull_request.* which the classifier treats as UNGUARDED_CONDITIONAL (vector-2, no partial credit). Same shape as omniclaude's check-handshake/boundary-parity findings (OMN-14856). skip_semantics: neutral_ok pending triage in OMN-14862."

  - name: "Release Dry Run Summary"
    mode: REQUIRED
    producer_kind: local
    workflow: release-dry-run.yml
    job_path: ["release-dry-run-summary"]
    skip_semantics: never
    rationale: "Job-level `if: always()` — provably true regardless of upstream job outcome. Live required status check on omnibase_compat dev branch protection."

  - name: "dry-run"
    mode: REQUIRED
    producer_kind: local
    workflow: release-dry-run.yml
    job_path: ["dry-run"]
    skip_semantics: never
    rationale: "Live required status check on omnibase_compat dev branch protection."

  - name: "Shim TTL Gate"
    mode: REQUIRED
    producer_kind: local
    workflow: shim-ttl-gate.yml
    job_path: ["shim-ttl-gate"]
    skip_semantics: never
    rationale: "Live required status check on omnibase_compat dev branch protection."

  - name: "Stale TODO Gate"
    mode: REQUIRED
    producer_kind: local
    workflow: stale-todo-gate.yml
    job_path: ["stale-todo-gate"]
    skip_semantics: never
    rationale: "Live required status check on omnibase_compat dev branch protection."

  - name: "Enforce validator-requirements.yaml (OMN-13291)"
    mode: REQUIRED
    producer_kind: local
    workflow: validate-validator-requirements.yml
    job_path: ["validate-validator-requirements"]
    skip_semantics: never
    rationale: "Live required status check on omnibase_compat dev branch protection. Withheld from main per 2026-07-20 branch-protection union PATCH (below the 5-PR proof bar on main at that time) — dev-only per this manifest's scope note."

  - name: "call / validate-docs"
    mode: REQUIRED
    producer_kind: cross_repo
    caller_workflow: docs-validate.yml
    caller_job: call
    cross_repo_ref: "OmniNode-ai/omnibase_core/.github/workflows/validate-docs.yml@main"
    skip_semantics: never
    rationale: "Markdown link validation (cross-repo reusable hosted in omnibase_core). Live required status check on omnibase_compat dev branch protection."

  - name: "non-dev-base-guard"
    mode: REQUIRED
    producer_kind: local
    workflow: non-dev-base-guard.yml
    job_path: ["non-dev-base-guard"]
    skip_semantics: never
    rationale: "Live required status check on omnibase_compat dev branch protection."

  - name: "pr-title / check-title"
    mode: REQUIRED
    producer_kind: cross_repo
    caller_workflow: pr-title-check.yml
    caller_job: pr-title
    cross_repo_ref: "OmniNode-ai/onex_change_control/.github/workflows/pr-title-check-reusable.yml@main"
    skip_semantics: never
    rationale: "PR title ticket-reference check (cross-repo reusable hosted in onex_change_control). Live required status check on omnibase_compat dev branch protection."

  - name: "main-target-guard"
    mode: REQUIRED
    producer_kind: local
    workflow: main-target-guard.yml
    job_path: ["main-target-guard"]
    skip_semantics: never
    rationale: "Live required status check on omnibase_compat dev branch protection."

  - name: "receipt-honesty"
    mode: REQUIRED
    producer_kind: local
    workflow: receipt-honesty.yml
    job_path: ["receipt-honesty"]
    skip_semantics: never
    rationale: "Live required status check on omnibase_compat dev branch protection."

  - name: "verify / verify"
    mode: REQUIRED
    producer_kind: cross_repo
    caller_workflow: call-receipt-gate.yml
    caller_job: verify
    cross_repo_ref: "OmniNode-ai/omnibase_core/.github/workflows/receipt-gate.yml@main"
    skip_semantics: never
    rationale: "Enforcement by Receipt (OMN-10483, cross-repo reusable hosted in omnibase_core). Live required status check on omnibase_compat dev branch protection."

  - name: "required-check-skip-guard / check-skip-vectors"
    mode: ADVISORY
    producer_kind: cross_repo
    caller_workflow: required-check-skip-guard-caller.yml
    caller_job: required-check-skip-guard
    cross_repo_ref: "OmniNode-ai/omniclaude/.github/workflows/required-check-skip-guard-reusable.yml@dev"
    skip_semantics: never
    rationale: "OMN-14858: this guard's own required check, fanned out from the OMN-14854 omniclaude canary. Runs on every PR but is NOT YET in required_status_checks — per the canary's ordering, the branch-protection flip happens only after 2 independent green runs on dev, tracked as a follow-up. mode stays ADVISORY until that flip lands (same PR as the flip, per schema-v3 rule: mode changes and branch-protection changes land together). Pinned to @dev not @main: omniclaude's main has not yet been promoted past OMN-14854 (verified 2026-07-20 via raw.githubusercontent.com 404 on main / 200 on dev) — flip to @main once promoted."

# Follow-up (not resolved in OMN-14858): a privileged
# required-check-manifest-reconcile.yml (push/schedule/workflow_dispatch only,
# never PR-triggered) comparing this file's REQUIRED rows against LIVE
# required_status_checks.contexts for `dev` needs a fine-grained PAT with
# Administration:Read on omnibase_compat (mirrors omniclaude's
# BRANCH_PROTECTION_PAT gap). Not provisioned in this PR; tracked as an open
# follow-up, same disposition as the omniclaude canary.
