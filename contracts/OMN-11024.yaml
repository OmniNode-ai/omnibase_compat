---
schema_version: "1.0.0"
ticket_id: OMN-11024
title: "Add canonical delegation wire DTO package in omnibase_compat"
summary: "Add canonical delegation wire DTO package in omnibase_compat"
is_seam_ticket: false
interface_change: true
interfaces_touched:
  - public_api
evidence_requirements:
  - kind: "ci"
    description: "omnibase_compat validation and focused delegation wire DTO tests pass"
    command: "uv run python scripts/validate_no_upstream_deps.py && uv run python scripts/check_compat_retention.py
      && uv run ruff check src/ && uv run mypy src/omnibase_compat --strict && uv run pytest src/omnibase_compat/tests/contracts/delegation/wire/test_delegation_wire_dtos.py
      -q && uv build"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "PR is open against main"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view {pr} --repo {repo} --json state,baseRefName -q '[.state, .baseRefName]
          | @tsv'"
  - id: "dod-002"
    description: "Repo-local OMN-11024 contract artifact is present"
    source: "generated"
    checks:
      - check_type: "file_exists"
        check_value: "contracts/OMN-11024.yaml"
  - id: "dod-003"
    description: "Focused delegation wire DTO tests pass"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "uv run pytest src/omnibase_compat/tests/contracts/delegation/wire/test_delegation_wire_dtos.py
          -q"
